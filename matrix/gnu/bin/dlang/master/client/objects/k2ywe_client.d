module matrix.gnu.bin.dlang.master.client.objects.k2ywe_client;

import std.algorithm;
import std.array;
import std.conv;
import std.stdio;

void main() {
    const char[] input = "Hello, World!";
    const auto input_bytes = input;
    const auto encoded_bytes = input_bytes;
    writeln("Encoded: ", encoded_bytes);
    const auto decoded_bytes = encoded_bytes;
    const auto decoded_string = decoded_bytes;
    writeln("Decoded: ", decoded_string);
}

